generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model VideoSubmission {
  id           Int      @id @default(autoincrement())
  originalPath String
  status       String   @default("pending")
  createdAt    DateTime @default(now())
}

model Video {
  id           Int      @id @default(autoincrement())
  originalName String
  url          String
  size         Int
  mime         String
  createdAt    DateTime @default(now())
  /** ブラウザが保持する所有者シークレット（未設定の旧データは変更不可） */
  ownerSecret  String?
  /** true のとき誰でも GET 可。新規作成は false（所有者のみ GET） */
  isPublic     Boolean  @default(true)

  lyricLines   LyricLine[]
}

model LyricLine {
  id        Int      @id @default(autoincrement())
  videoId   Int
  index     Int
  text      String
  startSec  Float?
  endSec    Float?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  video     Video    @relation(fields: [videoId], references: [id], onDelete: Cascade)

  @@unique([videoId, index])
  @@index([videoId])
}